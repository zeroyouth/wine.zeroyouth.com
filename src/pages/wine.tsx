import type {NextPage} from "next";

const WinePage: NextPage = () => { //타입스크립트에서 타입선언
    return (
        <div>
            <h1>Wine</h1>
        </div>
    )
}

export default WinePage;
//가장 기본적인 템플릿임. TypeScript를 쓰는